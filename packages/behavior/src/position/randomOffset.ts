// import { defineBehavior } from '@meshgradients/core'

// interface Options {
//   min: number
//   max: number
// }

// export const randomOffset = (options: Options) => defineBehavior((context) => {})
